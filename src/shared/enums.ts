export enum HoleListMode {
  random = 'random',
  timeline = 'timeline',
}

export enum HoleDetailCommentMode {
  all = 'all',
  author = 'author',
}

export enum HoleDetailCommentOrderMode {
  favorite = 'favorite',
  time = 'time',
}

export enum Role {
  User = 'user',
  Admin = 'admin',
  Banned = 'banned',
}
